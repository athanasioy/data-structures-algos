"""
Show a tree achieving the worst-case running time for algorithm depth.

Answer:

The depth algorithm has the worst running time in cases where the
tree has a single branch, as such the depth of the leaf node is 
equal to N. As a result, the calculation of depth is O(N).

For example, the tree A->B->C->D is a tree that the depth
algorithm is O(N).


"""
