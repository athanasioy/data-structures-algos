"""

Give a precise and complete definition of the concept of matching for
grouping symbols in an arithmetic expression. Your definition may be
recursive.

Answer:

An arithmetic expression has matching grouping symbols if
the opening group symbol has the corresponding closing
group symbol and the arithmetic expression inside and outside the
grouping symbol has also matching grouping symbols.

The word "corresponding" means, in this context,
of the same grouping type.

If the arithmetic expression has no grouping symbols, it
is considered as an arithmetic expression with matching
grouping symbols.


"""
